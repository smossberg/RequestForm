<%= form_for [@headerable,@heading], url: business_context_headings_path(@headerable, @heading), remote: true do |f| %>
	<%= f.text_field :title %>
	<%= f.text_field :text %>
	<%= f.submit  %>
<% end %>
