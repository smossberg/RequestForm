<div class="row">
        <div class="col-sm-8" id="headings">
		<div heading-id="<%= heading.id %>">
			<h3><%= heading.title %></h3>
			<p><%= heading.text %> </p>
		
		<%= link_to 'Edit', edit_business_context_heading_path(@bc,heading), remote: true %>
		</div>
	</div>
</div>
