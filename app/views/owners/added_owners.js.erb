<%# ownerable = @ownerable %>
<% if local_assigns.has_key? :removeable %>
	$("#owners").html("<%= j (render owners, ownerable: ownerable, removeable: true) %>");
<% else %>
	
	$("#owners").html("<%= j (render owners, ownerable: ownerable, removeable: true) %>");
<% end %>
$("#existing-owner-form").replaceWith("<%= j (render 'owners/existing_owners',:ownerable => @ownerable, :request => @request )%>");
